@model List<AboutLanguage>
<div class="container">
    <div class="row">
        <div class="col-10 mx-auto">
            @foreach (AboutLanguage aboutLanguage in Model)
            {
                @Html.Raw(aboutLanguage.Text)
            }
        </div>
    </div>
</div>