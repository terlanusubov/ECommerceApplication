@model List<TermsLanguage>
@{
    ViewData["Title"] = "Index";
}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Terms And Conditions</h4>
                    @if (Model.Count == 0)
                    {
                        <a href="@Url.Action("add","terms")" class="float-right btn btn-primary">Add</a>
                    }
                </div>
                <div class="table-responsive">
                    <table class="table">
                        <thead class="thead-light">
                            <tr>
                                <th scope="col">Text</th>
                                <th scope="col">Edit</th>
                                <th scope="col">Delete</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (TermsLanguage termsLanguage in Model)
                            {
                                <tr>
                                    <td>@termsLanguage.Data ...</td>
                                    <td>
                                        <a asp-controller="Terms" asp-action="Edit" asp-route-id="@termsLanguage.TermsId"><i class="fas fa-edit"></i></a>
                                    </td>
                                    <td>
                                        <a onclick="return confirm('Are you sure you want to delete this item?');" asp-controller="Terms" asp-action="Delete" asp-route-id="@termsLanguage.TermsId"><i class="fas fa-times"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>





